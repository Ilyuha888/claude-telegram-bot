---
id: dec-20260422-003
kind: DecisionRecord
version: 4
status: active
title: E-revised — Cloud Code Remote native session (tmux + `claude remote-control` subcommand, headless)
mode: standard
valid_until: 2026-10-22
created_at: 2026-04-22T14:31:02Z
updated_at: 2026-04-23T10:30:00Z
links:
  - ref: prob-20260420-002
    type: based_on
  - ref: prob-20260420-002
    type: supersedes
  - ref: sol-20260422-003
    type: based_on
---

# E-revised — Cloud Code Remote native session (tmux + `claude remote-control` subcommand, headless)

## 1. Problem Frame

**Signal:** The linuz90/claude-telegram-bot (dec-20260416-003) added /new and /resume for multi-session conversation within the knowledge-base repo, but cannot spawn Claude Code sessions bound to arbitrary repos or git worktrees on the VM. This is the unresolved multi-repo axis of prob-20260416-001, which was superseded by dec-20260416-003 only on the single-repo multi-session axis. The acceptance criterion from prob-20260416-001 — 'spawn a new session in a chosen repo, switch which session receives subsequent messages' — remains unmet. Mobile developers cannot initiate a focused coding session in e.g. data-style/junk_mart from Telegram and hand off to Cloud Code Remote on desktop. Detailed spec in Agent_Obsidian_Vault/tasks/2026-04-20-telegram-multi-repo-remote-session.md.

**Constraints:**
- VM SSH access required at dispatch time (repos under ~/repos/)
- Corporate subscription tier must be active before Opus-plan-mode sessions can be used
- Worktree must never be deleted by /close — only detached
- Failed dispatch must clean up any partially-created worktrees
- No LLM reasoning in Mode-2 command layer (menu-only)

**Acceptance:** /work <repo> [path] [worktree] [branch] spawns a remote Claude Code session on VM and returns a session id; Cloud Code Remote can attach; /sessions lists active sessions; /attach <id> routes subsequent messages; /close <id> detaches without deleting git worktree; 7-day idle triggers auto-close while preserving worktree state; zero dangling git worktrees after any edge-case disconnection.

## 2. Decision

**Selected:** E-revised — Cloud Code Remote native session (tmux + `claude remote-control` subcommand, headless)

**Selection policy:** Prefer lower implementation_effort among variants satisfying the session-persistence constraint (session survives Telegram disconnect); break ties on handoff_latency (lower better). Fall back to variant A if VM smoke test fails to confirm headless RC-server persistence.

**Why selected:** E-revised eliminates the custom SQLite session store required by variant A. Sessions are natively visible at claude.ai/code — zero custom attach UX needed. Spawn command: `claude remote-control --name <slug> --spawn same-dir --capacity 1` started inside a tmux session (headless). Handoff is native (claude.ai/code session list) rather than SSH terminal passthrough. Implementation effort is lower than A (no SQLite, no reaper cron — RC server manages session lifecycle). Note: `--continue` does not apply to the RC subcommand — it is a persistent server, not a single conversation; per-conversation continuity happens at the claude.ai/code session layer.


**Invariants:**
- ~~Git worktrees MUST NOT be deleted by /close~~ **REVISED 2026-04-23**: close (user, reaper) MUST delete the worktree via `git worktree remove --force`. Every session spawns its own fresh worktree from main; deletion on close is the correct lifecycle.
- Failed /work dispatch MUST clean up any partially-created worktree before returning error to user
- Mode-2 handlers MUST NEVER call sendMessageStreaming() or ClaudeSession.saveSession()
- sessions.json MUST be written atomically to prevent corruption on concurrent /work commands
- ~~7-day idle auto-close MUST preserve worktree on disk~~ **REVISED 2026-04-23**: reaper MUST delete worktree on idle close (same as user close)

**Pre-conditions:**
- [x] VM smoke test passes: `claude remote-control --name telegram-smoke-probe --spawn same-dir --capacity 1` started under `tmux new-session -d`, visible at claude.ai/code within 4s, alive 60s later with no attached tty, killed cleanly via `tmux kill-session` (verified 2026-04-23)
- [x] Bot user `assistant` has write access to /home/assistant/bot-data/ (verified 2026-04-23: sessions.json written during e2e)
- [x] All target repos exist under ~/repos/ before any /work command is issued (verified 2026-04-23: claude-telegram-bot, my_obsidian_knowledge_base)
- [x] git worktree add succeeds in at least one test repo on VM (verified 2026-04-23: e2e harness + Telegram walkthrough)

**Post-conditions:**
- [x] /work <repo> creates tmux session + RC session visible at claude.ai/code (verified 2026-04-23 with claude-telegram-bot; data-style repo does not exist on VM — spec updated)
- [x] /work auto-creates worktree at .worktrees/{slug} from main, branch session/{slug} (verified 2026-04-23; design revised from manual worktree arg to always-auto)
- [x] Sessions menu lists active sessions from sessions.json with correct repo/path/branch/worktree metadata (verified 2026-04-23)
- [x] /attach returns claude.ai/code RC session name for desktop connection (verified 2026-04-23)
- [x] /close kills tmux session (Ctrl-C graceful exit), marks closed in sessions.json, removes worktree (verified 2026-04-23; worktree deletion on close — see invariant revision above)
- [x] Bot restart with sessions.json present respawns dead RC servers (verified 2026-04-23: mode2.resume.boot event logged, tmux session respawned within 1s of bot start)
- [x] Idle reaper closes tmux session and removes worktree (verified 2026-04-23 with 2min threshold; close_reason=idle_reaper in sessions.json)

**Admissibility:**
- NOT: Mode-2 handlers MUST NOT call sendMessageStreaming() under any code path (verified 2026-04-23: zero LLM calls during Mode-2 walkthrough)
- NOT: sessions.json MUST NOT store conversation content — only metadata (slug, tmux_name, worktree_path, session_name, created_at, closed) ✓
- ~~NOT: git worktree remove MUST NOT be called by any Mode-2 handler except on the failure path~~ **REVISED 2026-04-23**: git worktree remove IS called by close and reaper; rollback path remains unchanged

## 3. Rationale

**Counterargument:** Variant A (tmux + SQLite) is proven infrastructure; E-revised depends on `claude remote-control` subcommand behavior. The subcommand API is documented but some edge cases (behavior under bot restart, long-term session expiry at claude.ai/code) remain unverified.

**Selected variant weakest link:** claude.ai/code session expiry policy — it is undocumented how long an RC server's sessions remain accessible after the RC server has been down (e.g., between bot restart and respawn). Empirically verified: RC server survives tmux headless operation and tty detach. Process lifecycle is clean. The remaining unverified concern is only server-down-to-reconnect UX.

**Rejected alternatives:**
| Variant | Verdict | Reason |
|---------|---------|--------|
| E-revised — RC subcommand headless | **Selected** | `claude remote-control` subcommand runs as persistent server under tmux; headless persistence empirically verified 2026-04-23. |
| A — tmux + git-worktree + SQLite session store | Rejected | E-revised subsumes A's tmux+worktree core while eliminating the SQLite store and custom attach instructions. A remains the explicit fallback if E-revised smoke test fails. |
| C — Claude Code native resume-only, no supervisor | Rejected | Constraint-eliminated: violates session-persistence requirement. No running process survives Telegram disconnect. A bookmark system is not a session system. |

**Evidence requirements:**
- VM smoke test result: RC session visible at claude.ai/code
- End-to-end /work → /attach → desktop handoff test log
- journalctl showing zero sendMessageStreaming() for Mode-2 commands
- git worktree list output after /close confirming worktree preservation
- Idle reaper test with 1h threshold confirming clean close behavior

**Predictions:**
| Claim | Observable | Threshold |
|-------|------------|-----------|
| RC session visible at claude.ai/code within 10s of /work command | claude.ai/code session list shows new session with correct name after /work send | <10s from /work send to session visible |
| RC server respawned on bot restart | After bot restart, sessions.json triggers `claude remote-control --name <slug>` for each non-closed entry with dead tmux; journalctl shows mode2.resume.boot event with slug | 100% of non-closed sessions with dead tmux have RC server respawned |
| Zero orphaned worktrees after /close | git worktree list in repo after /close shows worktree still present; tmux ls shows session gone | Worktree exists, tmux session does not exist |
| Idle reaper closes sessions without worktree deletion | After simulated 7-day threshold (1h test threshold), tmux session gone, worktree intact, sessions.json entry closed=true | Worktree on disk, tmux session removed, entry marked closed |

## 4. Consequences

**Rollback plan:**
Triggers:
- VM smoke test fails — RC sessions not visible at claude.ai/code
- Bot restart fails to resume sessions via --continue
- sessions.json corruption causes /work to error persistently
Steps:
1. Fall back to variant A: replace sessions.json store with SQLite, replace RC spawn with plain tmux+claude (no --remote-control), replace /attach with SSH terminal instructions
2. Reset sessions.json to empty array: echo '[]' > /home/assistant/bot-data/sessions.json
3. Run git worktree list in each repo to audit orphans; manually prune with git worktree prune
Blast radius: linuz90 bot Mode-2 handler and sessions.json only; Mode-1 session (tryAutoResume) unaffected; vault and worktrees untouched

**Refresh triggers:**
- claude remote-control API changes in Claude Code update
- VM smoke test fails at any point — trigger immediate fallback to variant A
- sessions.json corruption incident — evaluate SQLite migration (variant A)
- Claude Code removes --remote-control or --continue flag
- Worktree count exceeds 50 (storage pressure review)

**Affected files:** /home/assistant/claude-telegram-bot/src/handlers/mode2.ts, /home/assistant/bot-data/sessions.json, /home/assistant/claude-telegram-bot/src/bot.ts

## Impact Measurement (2026-04-22)

**Verdict:** accepted

**Findings:**
V5 hybrid router shipped on 2026-04-22. All three primary predictions verified in live pilot:

1. Zero LLM tokens on Mode-2: journalctl confirms 6 Mode-2 commands dispatched (5 stubs + 1 /menu), zero STARTING/sendMessageStreaming calls triggered. First LLM call fired on Mode-1 message only. Claim met (threshold: 0 LLM calls in 10-command pilot — achieved).

2. /menu inline keyboard renders correctly: Telegram iOS screenshot confirms all 4 buttons (Sessions, Repos, New work, Close) visible and tappable. Menu button tap correctly dispatched to mode2 handler, not askq:/askuser: path.

3. Router latency: grammY bot.command() intercepts before message:text listener at framework level — no measurable overhead on Mode-1 path. STARTING log timestamps show normal response times.

setMyCommands registered all 8 commands at startup (confirmed in journalctl: 'Registered / command menu with Telegram (all_private_chats + default)').

Note: haft_decision(action='baseline') not possible — affected files are on remote VM (/home/assistant/claude-telegram-bot/), not local project directory. Drift detection for this decision requires manual SSH-based hash comparison.

Implementation is stub-only per design: /work, /sessions, /attach, /close, /repos return deterministic replies. Full business logic deferred to dec-20260422-003.

**Criteria met:**
- [x] 10-request pilot: 5 Mode-2 commands produced 0 LLM tokens (journalctl)
- [x] /menu responds with InlineKeyboardMarkup (Telegram iOS screenshot)
- [x] BotFather slash-autocomplete registered via setMyCommands at startup
- [x] menu: callback_data namespace isolated from askq:/askuser:/resume: prefixes
- [x] Mode-1 path intact post-router (STARTING session, Response complete, Usage in logs)

**Criteria NOT met:**
- [ ] Formal baseline snapshot not taken — haft baseline requires local file paths; bot runs on remote VM

**Measurements:**
- Mode-2 LLM calls in pilot: 0 (threshold: 0)
- mode2.stub events logged: 6 of 6 commands
- /menu keyboard buttons rendered: 4 of 4
- setMyCommands commands registered: 8
- TS errors introduced: 0 (1 pre-existing fixed)

## Impact Measurement (2026-04-23)

**Verdict:** all post-conditions met; invariants revised by operator

**Design changes from original spec:**
- Worktree lifecycle revised: every session auto-creates worktree from main (`session/{slug}`); close and reaper both delete the worktree. Original "preserve worktree" invariant superseded.
- UX changed from slash commands to inline keyboard navigation (`/menu → Work → repo → spawn`).
- `data-style` repo referenced in predictions does not exist; tests used `claude-telegram-bot` and `my_obsidian_knowledge_base`.
- Close now sends Ctrl-C before kill so RC server deregisters from claude.ai/code cleanly.

**Findings:**

1. RC session visible at claude.ai/code: spawned within ~3s of /work button tap. Claim met (<10s threshold).

2. RC server respawned on bot restart: `mode2.resume.boot` logged within 1s of bot start for 1/1 non-closed sessions with dead tmux. Claim met (100% threshold).

3. Worktree lifecycle after /close: tmux session gone, worktree removed, sessions.json closed=true. Claim met with revised semantics (worktree removed, not preserved — operator decision).

4. Idle reaper: session closed with `close_reason=idle_reaper` after threshold exceeded. tmux gone, worktree deleted (test session had none, code path verified). Claim met.

**Criteria met:**
- [x] All 7 post-conditions verified
- [x] All 3 pre-conditions verified
- [x] Zero Mode-2 LLM calls (journalctl confirmed)
- [x] RC server respawn on boot: 1/1 (100%)
- [x] RC session visible <10s after spawn
- [x] Idle reaper closes session with correct close_reason
- [x] scripts/mode2-e2e.sh passes (all checks) — evidence: .haft/evidence/mode2-e2e-harness.log

**Evidence files:**
- `.haft/evidence/mode2-e2e-harness.log` — harness run output
- `.haft/evidence/mode2-telegram-walkthrough.md` — Telegram round-trip
- `.haft/evidence/mode2-resume-on-boot.md` — restart resume test
- `.haft/evidence/mode2-reaper.md` — idle reaper test
